import { agregar, eliminar, buscar } from "./usuarios.js";

/*
  console.log('--- MENU ---');
  console.log('1. Agregar usuario');
  console.log('2. Eliminar usuario');
  console.log('3. Buscar usuario');
  console.log('4. Salir');

    var opcion = prompt("Elige una opcion");

  switch (opcion) {
    case '1':
      rl.question('Ingrese el id de usuario: ', (idusuario) => {
        rl.question('Ingrese el nombre de usuario: ', (nome) => {
          usuarios.agregarUsuario(idusuario, nome);
          mostrarMenu();
          solicitarOpcion();
        });
      });
      break;
    case '2':
      rl.question('Ingrese el id de usuario a eliminar: ', (idusuario) => {
        usuarios.eliminarUsuario(idusuario);
        mostrarMenu();
        solicitarOpcion();
      });
      break;
    case '3':
      rl.question('Ingrese el id de usuario a buscar: ', (idusuario) => {
        usuarios.buscarUsuario(idusuario);
        mostrarMenu();
        solicitarOpcion();
      });
      break;
    case '4':
      rl.close();
      break;
    default:
      console.log('Opción inválida');
      mostrarMenu();
      solicitarOpcion();
      break;
  }
}

mostrarMenu();
solicitarOpcion();
*/