import { sumar, restar, dividir, multiplicar } from "./calculadora.js";

let primero = prompt("Escribe un numero");

let segundo = prompt("Escribe otro numero");

sumar(primero, segundo);

restar(primero, segundo);

dividir(primero, segundo);

multiplicar(primero, segundo);