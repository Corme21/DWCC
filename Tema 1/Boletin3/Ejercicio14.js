//Debuxar por pantalla con asteriscos o número de liñas que introduce o usuario
let lineas = prompt("Introduce la cantidad de lineas a dibujar");

for(let i = 0; i < lineas; i++){
    var escrito = "*";
    document.write(escrito);
}